DEFINE
    MEASURE 'Key Measures'[Operator Efficiency] = SUM(fLine_downtime[Downtime(min)])
	 // (Total Downtime for Operator) / (Number of Batches Managed)                     


EVALUATE
    SUMMARIZECOLUMNS(
        "Total Downtime", [Operator Efficiency]
    )